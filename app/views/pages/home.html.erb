<div class="outline">
  <h1>Welcome <%= current_user.first_name %></h1>
  <p>It's <%= Date.today.strftime('%A, %b %d') %></p>


  <% if @subscriptions.empty? %>
  <p>There are no subscriptions waiting for you.</p>

  <% else %>
    <p>There are <%= @subscriptions.count %> parcels waiting for you</p>
    <p>And <%= @skip_subscriptions.count %> houses have asked to be skipped</p>
    <p>They are:</p>
    <% @skip_subscriptions.each do |collection| %>
      <p><%= collection.subscription.user.first_name %> at <%= collection.subscription.street_address %> at </p>
    <% end %>
  <% end %>


</div>
