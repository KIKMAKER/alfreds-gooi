<div class="background">
  <div class="outline">
    <h1>Welcome <%= current_user.first_name %></h1>
    <p>It's <%= Date.today.strftime('%A, %b %d') %></p>
    <br>
    <% if current_user.driver? %>
      <p>Ready to get started?</p>
      <%= link_to "Start", drivers_days_start_path, class: 'collect-button' %>
    <% end %>
  </div>
</div>
