<div class="outline">
  <br>
  <%= simple_form_for :csv_upload, url: import_csv_path, method: :post, multipart: true do |f| %>
    <%= f.input :file, as: :file %>
    <%= f.button :submit, "Upload CSV", class: "collect-button" %>
  <% end %>
</div>
