<div class="outline">
  <br>
  <%= simple_form_for :csv_upload, url: import_csv_collecto_path, method: :post, multipart: true do |f| %>
    <%= f.input :file, as: :file %>
    <%= f.input :drivers_note, as: :text %>
    <%= f.button :submit, "Upload CSV", class: "gooi-button" %>
  <% end %>
</div>
