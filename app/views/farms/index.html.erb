<div class="page-container">

  <section class="partner-farms-section">
    <h1>Meet Our Partners<span class="yellow-txt">.</span></h1>
    <h5>Where Your Food Scraps Become Soil</h5>
    <div class="blog-text-section my-4">
      <p>
        Every week, Alfred collects organic waste from households across Cape Town and delivers it to our partner farms and composting sites.
        These organizations transform food scraps into rich, healthy soil that nourishes gardens and communities.
      </p>
    </div>
    <div class="farms-grid">
      <% @farms.each do |farm| %>
        <div class="farm-card-mini">
          <%= link_to farm_path(farm.slug || farm.id), class: "farm-card-link" do %>
            <div class="farm-image-container">
              <% if farm.photo.attached? %>
                <%= image_tag farm.photo, alt: farm.name, class: "farm-image" %>
              <% else %>
                <div class="farm-image-placeholder">
                  <i class="fa-solid fa-seedling"></i>
                </div>
              <% end %>
              <span class="collection-day-badge"><%= farm.collection_day %></span>
            </div>
            <div class="farm-card-content">
              <div class="d-flex justify-content-between align-items-center">
                <h3><%= farm.name %></h3>
                <p class="farm-suburb"><i class="fa-solid fa-location-dot"></i> <%= farm.suburb %></p>
              </div>
              <span class="farm-stat"><%= number_with_precision(farm.total_weight_kg || 0, precision: 0) %> kg collected</span>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </section>

  <div class="yellow-block">
    <h1>Ready to Add Your Scraps to the Next Compost Pile?</h1>
    <div class="mt-5">
      <%= link_to 'Sign Up', root_path(anchor: "pricing"), class: 'btn-gooi' %>
    </div>
  </div>
</div>
